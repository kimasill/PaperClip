# 테스트·검증 전략

타입체크·테스트·빌드를 언제 어떻게 돌릴지 정할 때 이 파일을 읽는다.

## 완료 전 필수(가능하면 전부)

```sh
pnpm -r typecheck
pnpm test:run
pnpm build
```

실행하지 못한 항목이 있으면 결과 보고에 **무엇을 왜 못 돌렸는지** 명시한다.

## 역할

- **typecheck**: 계약·타입 깨짐 조기 발견
- **test:run**: 회귀 방지
- **build**: 배포 가능한 산출물 확인
