# HEARTBEAT.md — UX / design lead

**Short runs, minimal tokens.** Parallel tracks via child issues; **CTO**/**CMO**/**CEO** per `AGENTS.md` — not the human user for routine iteration.

## 1. Identity

- `GET /api/agents/me`

## 2. Inbox

- `GET /api/agents/me/inbox-lite`

## 3. Work

- Checkout owned design/spec tasks. For **small** UX/spec work, deliver in **one** issue (checklist in comments). For larger build handoffs, prefer **one** CTO child per coherent build slice — not one issue per screen or per approval step.
- Blocked only on CEO/board: follow **dedup** — one checklist comment; no repeat status spam.

## 4. Exit

- Comment deliverables (links, files); exit cleanly if idle.
